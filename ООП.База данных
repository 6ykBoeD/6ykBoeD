    internal class Program
    {
        static void Main(string[] args)
        {
            DataBase players = new DataBase();
            players.AddPlayer("Artem", 2);
            players.AddPlayer("Boris", 3);
            players.AddPlayer("Semen", 4);
            players.BanPlayer(1);
            players.UnBanPlayer(1);
            players.DeletePlayer(2);

            for (int i = 0; i < players.Players.Count; i++)
            {
                players.Players[i].ShowInfo();
            }
        }
    }

    class Player
    {
        protected static int Ids ;
        public string Name { get; private set; }
        public int Level { get; private set; }
        protected int Id;
        protected bool IsBan;

        public Player()
        {

        }
        
        public Player(string name , int level , bool isBan = false)
        {
            Id = ++Ids;
            Name = name;
            Level = level;
            IsBan = isBan;
        }
    }
    class DataBase : Player
    {

        public List<DataBase> Players = new List<DataBase>();

        public DataBase()
        {

        }
        
        public DataBase(string name, int level): base(name , level)
        {

        }

        public void AddPlayer(string name, int level)
        {
            Players.Add(new DataBase(name, level));
        }

        public void ShowInfo()
        {
            if (IsBan == false)
            {
                Console.WriteLine($"id - {Id},Имя - {Name},Уровень - {Level}\n");
            }
        }

        public void BanPlayer(int numberId)
        {
            Players[numberId - 1].IsBan = true;
        }

        public void UnBanPlayer(int numberId)
        {
            Players[numberId - 1].IsBan = false;
        }

        public void DeletePlayer(int numberId)
        {
            Players.RemoveAt(numberId - 1);
        }
    }
