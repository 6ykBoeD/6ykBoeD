            string name;
            char symbol;
            int numberSimbols;
            int addSymbols = 2;
            Console.Write("Введите имя :");
            name = Console.ReadLine();
            Console.Write("Введите символ :");
            symbol = Convert.ToChar(Console.ReadLine());
            numberSimbols = name.Length + addSymbols;
            string outputSymbol = "";

            for (int i = 0; i < numberSimbols; i++)
            {
              outputSymbol += symbol;
            }
            Console.WriteLine(outputSymbol);
            Console.WriteLine($"{symbol}{name}{symbol}");
            Console.WriteLine(outputSymbol);
