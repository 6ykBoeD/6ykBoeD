            Random random = new Random();
            int minRangeNumber = 1;
            int maxRandgeNumber = 28;
            int randomNumber = random.Next(minRangeNumber, maxRandgeNumber);
            int minValue = 100;
            int maxValue = 999;
            int numberAttempt = 0;

            for (int i = 0; i <= maxValue; i += randomNumber)
            {
                if(i >= minValue)
                {
                    numberAttempt++;
                }               
            }

            Console.WriteLine($"Ваше число {randomNumber}");
            Console.WriteLine($"Количество трех значных натуральных чисел = {numberAttempt}");
